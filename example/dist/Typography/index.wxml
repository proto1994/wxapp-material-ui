<view 
  class="{{variant}} align-{{align}} color-{{color}} {{paragraph ? 'paragraph':''}} {{noWrap ? 'no-wrap':''}} classes" 
  style="{{styles}}"
>
  <slot></slot>
</view>

                            