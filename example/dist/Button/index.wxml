<button 
  class="{{buttonClass}} button-class"
  disabled="{{disabled}}"
  hover-class="{{buttonHoverClass}}"
  bindtap="bindtap"
  style="{{style}}"
  loading="{{loading}}"
  >
  <slot></slot>
</button>
