<button 
  class="{{buttonClass}} button-class"
  disabled="{{disabled}}"
  hover-class="{{buttonHoverClass}}"
  bindtap="bindtap"
  loading="{{loading}}"
  style="{{styles}}"
  >
  <slot></slot>
</button>
